<ol class="post-list">
  {{#each posts as |post|}}
  <li>
     {{> post-info post }}
     <div>
    {{#each posts as |post|}}
      {{> post-info post}}
      <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit post</a>
  </div>
  </li>
  {{/each}}
</ol>