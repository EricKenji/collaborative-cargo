<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/stylesheets/new.css" />
  <title>Collaborative Cargo</title>
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
  <link 
    rel="stylesheet" 
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
    rel="stylesheet" 
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" 
    crossorigin="anonymous">
</head>

<bod>
  <header class="mb-5">
    <h1 class="pt-4 mx-4" >Collaborative Cargo</h1>
    {{#if loggedIn}}
    <p class="username mx-4 text-white small" >Logged in as {{username}}</p>
    {{/if}}
    <nav class="navbar justify-content-around mt-5">
      <a class="font-weight-bold" href="/">Home</a>
      <a href="/users">Users</a>
      {{#if loggedIn}}
      <a href="/my-posts">My Posts</a>
      <a href="/create-post">Create Post</a>
      <button id="logout">Logout</button>
      {{else}}
      <a href="/login">Login</a>
      {{/if}}
    </nav>
  </header>


  <main class="container">
    <div class="row justify-content-around">
      {{{ body }}}
    </div>
  </main>

  
  {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>